# Make the Example Project executable

The Example Project will include small executable teaching examples, tests, documentation, and reproducibility evidence instead of remaining documentation-only. This makes its Issues, pull requests, Artifact Bundles, and engineering practices credible models for Fall AI Studio Fellows while keeping the project intentionally educational rather than production-scale; the former documentation-only September plan must be redesigned around a runnable baseline.
